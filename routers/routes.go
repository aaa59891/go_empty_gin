package routers

import (
	"github.com/gin-gonic/gin"
)

/*
	DELETE use query string for binding model and recording the history
	because the primary key's type of each model is different, use gin to bind it is more convenient
*/
func SetRoutes(r *gin.Engine) {

}
